target/
result
result-*
# Staged source copies (see README "Staged sources") — recreate via rsync.
# The lez-rln-src/Cargo.toml workspace shim IS committed (lint inheritance).
/logos-rust-sdk-src/
/rust-lib/lez-rln-src/rln-layouts/
